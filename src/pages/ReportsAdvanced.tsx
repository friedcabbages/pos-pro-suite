import { Navigate } from "react-router-dom";

export default function ReportsAdvanced() {
  // This feature is not production-ready yet; keep it fully hidden from the UI.
  return <Navigate to="/reports" replace />;
}
